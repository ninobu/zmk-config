name: 'Build firmware'
on:
  workflow_dispatch:
  push:
    paths:
      - 'build.yaml'
      - 'config/lily58.conf'
      - 'config/lily58.keymap'

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
